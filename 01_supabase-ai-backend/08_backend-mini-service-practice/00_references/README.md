# References

백엔드 미니 서비스 요구사항, API 설계, DB 설계, 테스트 기준을 정리합니다.

## 미니 서비스 체크리스트

수업에서 작은 서비스를 만들 때는 아래 순서로 확인합니다.

1. 사용자가 어떤 문제를 해결하려고 하는가?
2. 어떤 API endpoint가 필요한가?
3. 요청 데이터와 응답 데이터는 어떤 모양인가?
4. 어떤 데이터를 Supabase에 저장해야 하는가?
5. 어떤 상황에서 오류가 발생할 수 있는가?
6. 서비스 로그에는 무엇을 남겨야 하는가?
7. mock 구현과 실제 연동 구현을 어떻게 나눌 것인가?

## mock 먼저 구현하는 이유

초보자는 처음부터 Supabase와 LLM API를 모두 연결하면 오류 원인을 찾기 어렵습니다.

그래서 이 단원은 다음 순서로 진행합니다.

```text
mock LLM + 메모리 저장소
-> FastAPI endpoint 구조 확인
-> Supabase 테이블 준비
-> Supabase 저장 코드 연결
-> 실제 LLM API 연동은 이후 확장
```

이렇게 하면 API 구조, 데이터 저장 구조, 외부 API 호출 구조를 단계별로 이해할 수 있습니다.
