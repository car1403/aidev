# Legacy Docker PostgreSQL Redis Reference

이 폴더는 이전 백엔드 과정에서 사용하던 Docker/PostgreSQL/Redis 자료를 참고용으로 보관한 곳입니다.

현재 `01_supabase-ai-backend` 과정은 **Supabase 중심**으로 진행합니다.

## 현재 과정에서의 기준

```text
01_supabase-ai-backend
-> Supabase managed DB/API/Auth/RLS 중심
-> Docker/PostgreSQL/Redis 직접 실행은 진행하지 않음
```

## Docker는 어디서 배우나요?

Docker, Docker Compose, Redis, 운영형 서비스 구성은 아래 과정에서 본격적으로 다룹니다.

```text
C:\aidev\06_multi-agent-service-ops
```

## 이 폴더를 사용하는 경우

- 예전 자료를 비교하고 싶을 때
- PostgreSQL/Redis 로컬 실행 예시를 참고하고 싶을 때
- 06 과정에서 Docker 기반 운영으로 확장할 때

초보자 수업에서는 이 폴더를 먼저 열지 않아도 됩니다.

