# Lab 03 - Python PostgreSQL CRUD

Python 코드에서 로컬 PostgreSQL에 직접 연결하는 legacy lab입니다.

## 목표

- Python에서 PostgreSQL 연결 정보를 사용합니다.
- SQL을 실행해 데이터를 저장하고 조회합니다.
- 연결 문자열과 환경변수의 역할을 이해합니다.

## 현재 과정에서의 대응

현재 과정에서는 직접 DB 연결 문자열 대신 Supabase URL과 key를 사용합니다.

```text
SUPABASE_URL
SUPABASE_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY
```

Python CRUD는 아래 현재 과정 폴더에서 진행합니다.

```text
C:\aidev\01_supabase-ai-backend\06_supabase-db-and-auth\02_supabase-table-and-crud
```
