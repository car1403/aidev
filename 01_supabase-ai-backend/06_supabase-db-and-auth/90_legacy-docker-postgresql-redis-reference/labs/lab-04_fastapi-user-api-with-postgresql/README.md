# Lab 04 - FastAPI 사용자 API와 PostgreSQL 연결

FastAPI endpoint에서 로컬 PostgreSQL을 직접 사용하는 legacy lab입니다.

## 목표

- `/users` API를 설계합니다.
- FastAPI에서 DB 저장과 조회 흐름을 연결합니다.
- Swagger UI로 endpoint를 테스트합니다.

## 현재 과정에서의 대응

현재 과정에서는 FastAPI와 Supabase를 연결합니다.

```text
C:\aidev\01_supabase-ai-backend\06_supabase-db-and-auth\03_fastapi-supabase-integration
```

로컬 DB 접속 대신 Supabase SDK를 사용합니다.
