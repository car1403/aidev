# 인증과 접근 제어 기초

## 핵심 흐름

```text
회원가입 -> 비밀번호 해시 저장 -> 로그인 -> 토큰 발급 -> 보호된 API 접근
```

## 주의

- 비밀번호 원문은 저장하지 않는다.
- 토큰에는 필요한 최소 정보만 담는다.
- 사용자별 데이터는 현재 사용자 ID로 필터링한다.

## 수업 범위

이 단원에서는 인증의 기본 흐름을 익힙니다. 보안 심화는 이후 보안/운영 단원에서 다룹니다.

