# 02_local-fastapi backend 참고 패키지 목록
#
# 기본 설치는 03 과정 최상위 requirements.txt로 진행합니다.
#
#   C:\aidev\04_supabase-ai-mini-project\requirements.txt
#
# 이 파일은 해당 FastAPI 예제만 따로 실행하거나 배포할 때 필요한 패키지를
# 확인하기 위한 참고용 파일입니다.

# FastAPI API 서버를 만들기 위한 핵심 웹 프레임워크입니다.
# 이 예제에서는 /health 같은 간단한 API 엔드포인트를 만드는 데 사용합니다.
fastapi==0.115.6

# FastAPI 앱을 로컬 개발 서버로 실행하기 위한 ASGI 서버입니다.
# 예:
#   uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
uvicorn[standard]==0.32.1

# 04_supabase-ai-mini-project 최상위 .env 파일을 읽기 위해 사용합니다.
# APP_TITLE 같은 환경변수를 코드에서 읽을 수 있게 해줍니다.
python-dotenv==1.0.1

# FastAPI 요청/응답 모델을 정의하고 데이터 타입을 검증할 때 사용합니다.
# 이 예제에서는 작은 API라도 이후 Supabase API 설계와 같은 방식으로 확장하기 위해 포함합니다.
pydantic==2.10.4
