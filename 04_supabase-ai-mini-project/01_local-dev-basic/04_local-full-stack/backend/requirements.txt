# 04_local-full-stack backend 참고 패키지 목록
#
# 기본 설치는 04 과정 최상위 requirements.txt로 진행합니다.
#
#   C:\aidev\04_supabase-ai-mini-project\requirements.txt
#
# 이 파일은 로컬 풀스택 백엔드 예제만 따로 확인할 때 필요한 패키지를
# 설명하기 위한 참고용 파일입니다.

# FastAPI API 서버를 만들기 위한 핵심 웹 프레임워크입니다.
# 이 예제에서는 learning_logs 저장/조회/수정 API를 만드는 데 사용합니다.
fastapi==0.115.6

# FastAPI 앱을 로컬 개발 서버로 실행하기 위한 ASGI 서버입니다.
# 예:
#   uvicorn main:app --reload --host 127.0.0.1 --port 8000
uvicorn[standard]==0.32.1

# 04_supabase-ai-mini-project 최상위 .env 파일을 읽기 위해 사용합니다.
# Supabase URL/key를 코드에 직접 쓰지 않도록 도와줍니다.
python-dotenv==1.0.1

# Python에서 Supabase 테이블에 insert/select/update 요청을 보내기 위한 패키지입니다.
# 이 예제에서는 learning_logs 테이블과 연결됩니다.
supabase==2.10.0

# FastAPI 요청 Body와 응답 구조를 검증하기 위한 패키지입니다.
# LearningLogCreate, LearningLogUpdate 같은 모델을 만들 때 사용합니다.
pydantic==2.10.4
