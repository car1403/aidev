# 01_python-env-check

## 목표

Python과 `.venv` 환경을 확인합니다.

## 실행

```powershell
cd C:\aidev\09_ai-workflow-mini-project
.\.venv\Scripts\Activate.ps1
python --version
pip --version
pip install -r requirements.txt
```
