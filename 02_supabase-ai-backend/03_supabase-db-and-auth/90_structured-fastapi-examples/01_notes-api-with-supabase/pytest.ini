[pytest]
# FastAPI TestClient 내부 의존성에서 나오는 경고를 수업 출력에서 숨깁니다.
# 테스트 실패가 아니라 라이브러리 버전 경고이므로, 초보자 실습에서는 핵심 결과만 보이게 합니다.
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated.*:Warning
